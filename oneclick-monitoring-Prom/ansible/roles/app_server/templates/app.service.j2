[Unit]
Description={{ app_name }} Application
After=network.target

[Service]
User={{ ansible_user | default('ubuntu') }}
ExecStart=/usr/bin/java -jar {{ jar_result.files[0].path }}
Restart=always
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
