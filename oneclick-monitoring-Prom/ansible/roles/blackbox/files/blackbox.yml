modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      preferred_ip_protocol: ip4    # ✅ valid
      ip_protocol_fallback: true    # ✅ optional
      follow_redirects: true        # ✅ optional
      valid_status_codes: [200, 302]
