- name: daemon reload
  systemd:
    daemon_reload: yes

- name: restart prometheus
  systemd:
    name: prometheus
    state: restarted

- name: reload prometheus
  uri:
    url: http://localhost:9090/prometheus/-/reload
    method: POST
    status_code: 200